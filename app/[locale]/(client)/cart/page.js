import CartPage from '@/components/pages/cart/CartPage';

export const metadata = {
    title: 'Your Shopping Cart | GardenHub',
    description: 'Review the items in your cart and proceed to checkout.',
};

export default function Page() {
    return <CartPage />;
}
